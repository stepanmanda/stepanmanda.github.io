---
interface Props {
    num?: string;
    title: string;
    body: string;
}

const { title, body } = Astro.props;
---

<div class="step">
    <span class="step__dot" aria-hidden="true"></span>
    <h4 set:html={title}></h4>
    <p set:html={body}></p>
</div>
