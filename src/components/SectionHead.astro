---
interface Props {
    eyebrow?: string;
    title: string;
    lead?: string;
    maxLeadWidth?: string;
    titleMax?: string;
    reveal?: boolean;
}

const { eyebrow, title, lead, maxLeadWidth = '62ch', titleMax, reveal = true } = Astro.props;
const revealAttr = reveal ? 'data-reveal' : undefined;
---

<div class="section-head" {...revealAttr !== undefined ? { 'data-reveal': '' } : {}}>
    {eyebrow && <span class="eyebrow">{eyebrow}</span>}
    <h2 set:html={title} style={titleMax ? `max-width:${titleMax};` : undefined}></h2>
    {lead && <p style={`color:var(--text-secondary); font-size:1.1rem; max-width:${maxLeadWidth}; margin-top:var(--space-2);`} set:html={lead}></p>}
</div>
